/**
 * Shared constants used across main process and renderer.
 * @module constants
 */

/** Capture/OCR mode for member lists. */
export const MODE_MEMBER = 'member';

/** Capture/OCR mode for event lists. */
export const MODE_EVENT = 'event';
