# Dependencies
node_modules/

# Electron build output (too large for git, regenerated with npm run build)
mitglieder-extractor/dist/

# Playwright browsers (too large for git, regenerated with npx playwright install)
mitglieder-extractor/pw-browsers/

# OS files
Thumbs.db
Desktop.ini
.DS_Store

# Environment / secrets
.env
.env.local


# Tesseract trained data (downloaded automatically)
*.traineddata

# Extracted assets (too large for git)
extract/
assets/

# Runtime config files (contain credentials)
capture-config.json
mitglieder-extractor/mitglieder-config.json

# Runtime data (generated by the app)
mitglieder-extractor/captures/
mitglieder-extractor/results/
mitglieder-extractor/logs/

# Benchmark output (regenerated on demand)
mitglieder-extractor/test/results/

# Debug images and diagnostic scripts (regenerated on demand)
mitglieder-extractor/test/debug/

# Cursor / IDE
.cursor/

# Logs
*.log
npm-debug.log*



# Test fixtures: ground-truth.json is TRACKED (do NOT ignore)
# mitglieder-extractor/test/fixtures/ ← intentionally tracked for reproducible tests
